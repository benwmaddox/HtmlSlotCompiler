# Specs

Place implementation-ready specs here.

Conventions:

- Use sortable names such as `2026-03-16-watch-mode.md`.
- Prefix drafts with `draft-` so the loop ignores them.
- Include acceptance criteria and explicit verification commands.
